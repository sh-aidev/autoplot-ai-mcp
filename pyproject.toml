[project]
name = "mcp-server-template"
version = "0.1.0"
description = "A minimal MCP server template"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "mcp[cli]>=1.28.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "toml>=0.10.2",
]
